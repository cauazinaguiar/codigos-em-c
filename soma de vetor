#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <locale.h>

int main(void) {
		setlocale(LC_ALL, "Portuguese");
		int vetor[]={10,6,30,4,40};
		int i=0;
		int soma=0;

	for(i=0;i<5;i++){
		soma+=vetor[i];
	}
	printf("\n soma do vetor é: %d", soma);
		return 0;
	}
