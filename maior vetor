#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main(void) {
		setlocale(LC_ALL, "Portuguese");
		int vetor[]={10,6,30,4,40};
		int i=0;
		int maior=0;

	for(i=0;i<5;i++){
		if (vetor[i]>maior){
	
		maior=vetor[i];
	}
	}
	printf("\no maior vetor é: %d", maior);
		return 0;
	}
